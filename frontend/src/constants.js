// Evaluations categories used for scoring.
export const CATEGORIES = ['Technical', 'Communication', 'Problem Solving', 'Culture Fit', 'Experience']

// Valid candidate lifecycle statuses.
export const STATUSES = ['new', 'reviewed', 'hired', 'rejected']

// Default number of candidates per page.
export const PAGE_SIZE = 20

// Allowed score values (1-5 scale).
export const SCORE_VALUES = [1, 2, 3, 4, 5]
